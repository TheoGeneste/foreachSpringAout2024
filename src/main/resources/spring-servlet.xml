<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Configuration de la conversion JSON -->
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
        <property name="messageConverters">
            <list>
                <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter" />
            </list>
        </property>
    </bean>

    <!-- Définition du handler mapping sans annotations -->
    <bean class="org.springframework.web.servlet.handler.SimpleUrlHandlerMapping">
        <property name="mappings">
            <props>
                <!-- Mapping d'URL à un contrôleur spécifique -->
                <prop key="/Etudiant/*">etudiantController</prop>
            </props>
        </property>
    </bean>

    <!-- View Resolver (pour éviter d'utiliser des JSP ici car on retourne du JSON) -->
    <bean class="org.springframework.web.servlet.view.BeanNameViewResolver" />

    <!-- Configuration du EtudiantController -->
    <bean id="etudiantController" class="foreach.cda.Model.Etudiant" />

</beans>
